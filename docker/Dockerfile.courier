FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml README.md /app/
COPY src /app/src

RUN pip install --upgrade pip && pip install .[dev]

ENV PYTHONPATH=/app/src

EXPOSE 8200

ENTRYPOINT ["uvicorn", "courier.api:app", "--host", "0.0.0.0", "--port", "8200"]
